# .env is better suited for public variables, ie, variables that should not commited
# For secret variables is better to use DevServerControl tool with set_env_variable: ["KEY", "SECRET"]

# https://www.builder.io/c/docs/using-your-api-key
VITE_PUBLIC_BUILDER_KEY=__BUILDER_PUBLIC_KEY__
PING_MESSAGE="ping pong"

# eGov.kz OAuth Configuration
# Get your Client ID from: https://idp.egov.kz (eGov.kz Developer Portal)
VITE_EGOV_CLIENT_ID=freelancekz-app
# Note: Client Secret should be set via DevServerControl tool, not in .env
# VITE_EGOV_CLIENT_SECRET should be set using: set_env_variable: ["VITE_EGOV_CLIENT_SECRET", "your_secret"]

# API base URL (leave empty to use same origin)
# Example for local Flask backend: http://127.0.0.1:5001
VITE_API_BASE_URL=http://127.0.0.1:5001
